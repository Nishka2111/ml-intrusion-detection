
---

# `DATABASE.md`

For this one, I would be slightly careful about claiming exact column types or primary-key details unless your `models.py` confirms them. The documentation below describes the database behavior that your shown prediction route definitely uses.

```markdown
# Database Documentation

## 1. Overview

The ML-Based Network Intrusion Detection System uses a relational database to store intrusion detection results generated by the backend.

The backend uses:

```text
SQLite
